---
import { Header } from '../components/Header';
import { Footer } from '../components/Footer';

export interface Props {
	content: {
		title: string;
		description: string;
		pubDate?: string;
		updatedDate?: string;
		heroImage?: string;
		imageSource?: string;
	};
}

const {
	content: { title, pubDate, updatedDate, heroImage, imageSource },
} = Astro.props;
---

<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="grid justify-center mx-4">
		<main>
			<a href="/"><nav class="my-5 text-lg italic text-gray-400">← Home</nav></a>
			<article class="grid grid-cols-flow gap-4 max-w-lg">
				<h1 class="text-3xl font-semibold antialiased">{title}</h1>
				{pubDate && <time class="italic text-gray-400">{pubDate}</time>}
				{
					updatedDate && (
						<div>
							Last updated on <time>{updatedDate}</time>
						</div>
					)
				}
				<slot />
			</article>
		</main>
		<Footer />
	</body>
</html>
<style>
	@tailwind base;
	@layer base  {

		html {
			font-family: system-ui, sans-serif;
			background-color: #1F2028;
			color: #d4cfcf;
		}
		}
</style>